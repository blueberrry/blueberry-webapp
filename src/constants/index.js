import { BREAKPOINTS, GLOBALS, OFFSETS, SIZES, FONTS } from './theme';
import { RESETS } from './resets';
import { COLOURS } from './colours';
import { STEP_GRID, DROP_SHADOW } from './styleFragments';

const RANGE = {
  min: 2,
  max: 10,
};

const CURRENCY_SYMBOL = '£';

export { BREAKPOINTS, COLOURS, GLOBALS, OFFSETS, SIZES, STEP_GRID, FONTS, RESETS, RANGE, CURRENCY_SYMBOL, DROP_SHADOW };
